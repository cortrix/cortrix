"""PATCH /api/v1/admin/tenants/{id}/quota — success (Python SDK skeleton)."""
from cortrix import Client

client = Client(api_key="cx_live_xxx")

# TODO: call the corresponding client.<resource>.<verb>(...); see P04 § 8.3 + canonical examples (query/documents/memory)
