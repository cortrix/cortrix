# =============================================================
# F23 W4: OWASP ZAP baseline scan rules (design: F23-tests.md §3.2)
# Used by: zap-baseline.py -I -t http://localhost:8420 -c tests/security/zap-baseline.conf
#
# Format: <rule-id>\t<WARN|FAIL|IGNORE>\t(<comment>)
# Gate: High/Critical findings FAIL the nightly job (§6.3 hard gate).
# =============================================================

# --- Hard fails (OWASP Top 10 relevant to a JSON API) ---
40012	FAIL	(Cross Site Scripting (Reflected))
40014	FAIL	(Cross Site Scripting (Persistent))
40018	FAIL	(SQL Injection)
90019	FAIL	(Server Side Code Injection)
90020	FAIL	(Remote OS Command Injection)
6	FAIL	(Path Traversal)
7	FAIL	(Remote File Inclusion)

# --- Warnings (report, do not block) ---
10038	WARN	(Content Security Policy header — UI pages only)
10020	WARN	(Missing anti-clickjacking header — API responses)
10021	WARN	(X-Content-Type-Options missing)
10035	WARN	(Strict-Transport-Security — TLS terminates at Caddy in prod)
10063	WARN	(Permissions Policy header)

# --- Ignores (not applicable: headless JSON API behind Caddy) ---
10094	IGNORE	(Base64 disclosure — ULID/doc ids are expected opaque strings)
10109	IGNORE	(Modern Web Application informational)
